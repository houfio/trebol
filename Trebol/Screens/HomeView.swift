import SwiftUI

struct HomeView: View {
    var body: some View {
        Title(title: "Home")
    }
}

struct HomeView_Previews: PreviewProvider {
    static var previews: some View {
        HomeView()
    }
}
